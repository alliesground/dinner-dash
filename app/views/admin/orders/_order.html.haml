%tr{id:"#{order.id}"}
  %td= order.id
  %td.status= order.status
  %td.action
    - if order.ordered? || order.paid?
      = link_to "cancel", admin_order_path(order, order:{status:'cancelled'}), remote: true, method: :put, class: 'cancel'

    = link_to "show", admin_order_path(order)
