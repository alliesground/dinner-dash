%h3#cart_icon 
  Shopping Cart Items (
  %span= @cart.cart_items.count 
  )
  -# = "Shopping Cart Items (%span#{@cart.cart_items.count})"

-#
  - @cart.items.each do |item|
    %b= item.title

  %div{style: "padding-top: 10px;"}
    = link_to 'Clear cart', destroy_all_cart_items_path, method: :delete
