%h2 Your Cart Items

- @cart_items.each do |cart_item|
  %b= cart_item.item.title
  = form_with model: [:cart, cart_item], html: {style: "display: inline"}  do |f|
    = f.select(:quantity, [1,2,3,4,5], {}, onchange: 'Rails.fire(this.form, "submit")')
  = link_to 'Remove Item', cart_cart_item_path(cart_item), method: :delete
  %hr

%div{style: "padding-top: 10px;"}
  = link_to 'Clear cart', destroy_all_cart_cart_items_path, method: :delete

= button_to 'checkout', cart_checkout_path, method: :get
